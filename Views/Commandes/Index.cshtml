@model IEnumerable<Asp.net_MVC_TP_7.Models.Commande>

@{
    ViewBag.Title = "Index";
}

<h2 style="text-align:center">Liste Commandes</h2>

<p>
    @Html.ActionLink("Ajout", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Date_Cmd)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Client.Nom_Clt)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Date_Cmd)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Client.Nom_Clt)
        </td>
        <td>
            @Html.ActionLink("Modification", "Edit", new { id=item.Num_Cmd }) |
            @Html.ActionLink("Details", "Details", new { id=item.Num_Cmd }) |
            @Html.ActionLink("Supprission", "Delete", new { id=item.Num_Cmd })
        </td>
    </tr>
}

</table>
